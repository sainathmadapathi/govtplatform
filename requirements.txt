Flask>=3.0.0
flask-cors>=5.0.0


# OCR for scanned scorecards and photos (optional: without these the app still runs and
# says honestly that it cannot read a scan). ONNX models, no system binary needed.
rapidocr-onnxruntime>=1.3.0
pypdfium2>=4.0.0
pillow>=10.0.0
numpy>=1.24.0
